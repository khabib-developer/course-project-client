import Box from "@mui/material/Box";

export const MainPage = () => {
  return (
    <Box
      sx={{
        minHeight: "100vh",
        bgcolor: "background.default",
        color: "text.primary",
      }}
    ></Box>
  );
};
